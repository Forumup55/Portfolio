export default defineNuxtRouteMiddleware(async (to, from) => {
  // const {
  //   public: { tokenName, profileName },
  // } = useRuntimeConfig();
  // const profile = useCookie(profileName).value;
  // const token = useCookie(tokenName).value;

  // if (token && profile) {
  // } else {
  // }
});
